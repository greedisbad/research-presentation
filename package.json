{
  "name": "research-presentation-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build:full": "vite build --config vite.config.ts",
    "build:single": "vite build --config vite.single.config.ts",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "typescript": "~5.8.3",
    "vite": "^6.1.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
