# AI 辅助技术调研实践：扩展研究报告底稿

受众：有开发经验、会使用通用 AI 工具但未系统管理技术研究流程的开发者。
日期：2026-09-03。
范围：技术资料的搜索、筛选、证据核验、工程转化和知识沉淀；以全球公开资料为范围，以近三年 Agent 实践为主，并保留经典信息检索与软件工程研究。
排除：不做产品价格比较，不做最终演示稿，不把厂商自报效率数字当作普遍结论。

## 直接结论

AI 可以显著扩大检索面、压缩阅读和格式转换成本，并在有明确反馈回路时把资料直接转化为代码、实验和结构化知识。真正决定效果的不是“模型会不会搜索”，而是一组系统能力：问题建模、来源分层、证据追溯、可执行验证、权限边界、效果测量和仓库内沉淀。

没有这些条件时，AI 可能产生带引用但无充分支持的报告、复制不适用的参考代码，甚至增加资深开发者的完成时间。因此这场演讲不应宣传“把调研交给 AI”，而应教授“如何设计一条由 AI 承担规模、人类承担判断、测试承担裁决的研究流水线”。

这套方法并非只从本地案例归纳。Engelbart 的人机增强框架提供系统观；Bush 的 associative trails 提供可重走知识路径的历史线索；Kitchenham 的证据化软件工程和系统综述指南提供问题、协议和筛选的可审计原则；Keshav 的三遍阅读法提供分层阅读策略；Wineburg、McGrew 与 Caulfield 提供横向核验方法；Wilson 等提供自动化与可复现实践。Anthropic、OpenAI 与 Google 的现代 Agent 系统则展示这些原则如何进入规划、动态搜索、按需上下文、仓库知识和实验评价闭环。

## 证据合成

### 为什么要先研究再动手

开发者维护陌生系统时本来就需要定位相关代码、理解关系和收集信息。GitHub 官方源码阅读指南、Claude Code 的 Explore→Plan→Code→Commit 工作流，以及 OpenAI 对 Codex 代码理解用例的描述，都把理解现有环境放在修改之前。

WPS 本地案例证明这不是抽象建议：项目先分析 11 个参考项目，再根据 WPSJS、可编辑性和 JavaScript 运行环境筛选方案，最后只复用局部设计资产和接口模式。

### 为什么一次搜索不够

Query Expansion 研究表明术语不匹配会降低召回；Wohlin 的 snowballing 提供了从种子资料沿引用向前、向后扩展的方法。Anthropic 的生产研究系统则展示了一个动态版本：主代理拆分相互独立的问题，子代理根据中间发现继续检索，汇总者决定是否还有缺口。

### 为什么引用仍需核验

ALCE、SAFE、FACTS Grounding 与 DeepResearch Bench 都将回答正确性、引用准确性、引用完整性或 grounding 拆成不同维度。这意味着一个格式完美、链接很多的报告仍可能缺乏证据。

### 为什么“动手”是研究的一部分

WPS 图表、Word 段落和批量操作的失败都说明，文档与相邻项目只能提供假设。Google ERA 会搜索文献后写代码并按成功指标评价；AlphaEvolve 将生成程序交给自动 evaluator；SWE-bench 用真实测试判断 Issue 是否解决。共同原则是：对工程问题，执行反馈属于证据。

Deck.gl 本地案例提供了一个更具体的过程：连续生成未达到目标后，先将问题拆成对象边界、参数语义、生命周期和版本兼容，再让模型提出假设；官方文档确认公开契约，目标版本源码确认内部实现，同仓库可运行代码提供当前范式，最后由对照实验确认浏览器环境行为。案例中的具体 API 不应进入演讲主体，真正可迁移的是“来源要匹配问题、竞争假设要交给实验”。

### 为什么要把知识写回仓库

OpenAI Harness Engineering 强调仓库内、可版本化、可验证的知识；Spotify 的大规模迁移则依赖 lineage、代码搜索、标准化测试和细粒度边界。WPS 项目把研究、计划、失败和规则写回 `.planning/`、Learnings 和 `AGENTS.md`，使一次性发现能影响下一轮 Agent。

Dice 的三份连续复盘补充了负知识和交接机制：不只保存选中的方案，也保存已排除路线、不要重复搜索的内容、仍待验证的风险以及任务 brief。评审结论也不直接作为真相，而要由文件、diff、测试或复现作 Accept / Reject / Defer 裁决。

### 为什么必须保留反例

METR 的随机对照研究显示，在其特定的资深开发者、熟悉仓库和早期 2025 工具条件下，AI 平均使任务变慢。DORA 将 AI 描述为组织系统的放大器。二者共同限制了任何“AI 必然提效”的结论：收益必须在具体任务中测量，审核与等待也是成本。

### 为什么公开仓库不等于可直接复用

GitHub 官方许可证文档指出，无许可证的公开仓库仍受默认版权保护。参考代码还必须区分阅读学习、复制改写和引入依赖三种关系，并增加版本、维护、供应链与退出成本检查。Code Search、Issue、linked PR、Blame 和 Release 可以共同回答“代码如何实现、为何修改、是否已交付”，OpenSSF Scorecard、Dependency Graph 与 OSV 则提供依赖风险线索。

### 为什么自动搜索也需要安全边界

NIST、Anthropic、Microsoft 与 OWASP 均记录了间接提示注入风险：恶意指令可以藏在 Agent 读取的网页、文档或工具输出中。研究系统必须把外部内容视为不可信数据，并在模型外实施内容隔离、最小权限、数据流约束和高后果确认。

### 如何知道研究已经足够

停止条件不应是“搜不到新链接”，而应是高风险问题已有一手证据、反证不再改变决策、扩展查询进入饱和，或剩余问题只能通过实验回答。Cochrane 的搜索指南要求记录停止理由，并把引用追踪仍大量发现新材料视为原策略可能不足的信号。这里借用其可审计思想，而不声称普通技术调研具有系统综述的完备性。

## 当前最适合演示的案例组合

1. WPS Phase 02：11 个项目如何收敛成三条可行方案。
2. WPS Phase 07：经过实现后明确放弃图表路径。
3. WPS Phase 09：Prompt 批量规则失效，能力下沉为数组工具参数。
4. 视频案例：65 词查询矩阵如何减少单一关键词盲区。
5. 视频案例：字幕不可用后进入音频下载与 ASR，再回到官方来源核验。
6. 外部反例：METR 主观提效感与实测结果的差异。
7. 安全反例：网页中的隐藏指令为何不能直接进入有权限的 Agent。
8. 版本反例：PR 已合并但当前 Release 尚未包含修复。
9. Deck.gl：从生成失败拆出研究问题，再追到文档、同版本源码和对照实验。
10. Dice 短例：用“已确认／已排除／待验证”保存跨会话调研记忆。

## 重要限制

- 厂商工程博客是一手实践但存在选择偏差。
- 2025–2026 Agent 能力变化快，产品能力与效率数字在正式演示前需要复核。
- 学术文献检索的封闭语料和明确评价，与开放 Web 技术调研并不完全相同。
- 视频资料的中文文件名已通过 `ditto` 正确恢复，但候选视频的网页元数据、转写原句与总结仍需三方抽样核验。
- WPS 项目没有随下载包携带 Git 历史，不能用提交比例证明 AI 代码占比。

## 推荐后续

- 继续补全 WPS “参考项目→借用资产→实现文件→测试/失败证据”映射中的 commit/版本信息。
- 为一个 S 级视频建立“网页元数据→转写原句→总结主张→外部证据”的完整资料卡。
- 用真实小任务试跑现有模板与评测表，记录人工主动时间、核验成本和返工。
- 补企业内部资料的分级、脱敏和允许使用的模型/服务边界样例。
- 等材料稳定后再设计 30 分钟与 60 分钟两个演讲版本。

详细引用见 `docs/sources/claim-ledger.md` 与 `docs/sources/bookmarks.md`。
